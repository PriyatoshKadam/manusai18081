'use client';

import { useEffect, useState } from 'react';
import { useSearchParams } from 'next/navigation';
import Link from 'next/link';

export default function IntegrationsPage() {
  const search = useSearchParams(); const siteId = search.get('siteId');
  const [site, setSite] = useState<any>(null); const [policy, setPolicy] = useState<any>(null); const [deliveries, setDeliveries] = useState<any[]>([]); const [webhooks, setWebhooks] = useState<any[]>([]); const [url, setUrl] = useState(''); const [secret, setSecret] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const [testing, setTesting] = useState(false);
  async function load() { if (!siteId) return; const qs = `siteId=${encodeURIComponent(siteId)}`; const [siteResponse, policyResponse, deliveryResponse, webhookResponse] = await Promise.all([fetch(`/api/sites/${siteId}`, { cache: 'no-store' }), fetch(`/api/alert-policy?${qs}`, { cache: 'no-store' }), fetch(`/api/alert-deliveries?${qs}`, { cache: 'no-store' }), fetch(`/api/webhooks?${qs}`, { cache: 'no-store' })]); const [siteBody, policyBody, deliveryBody, webhookBody] = await Promise.all([siteResponse.json(), policyResponse.json(), deliveryResponse.json(), webhookResponse.json()]); if (!siteResponse.ok) throw new Error(siteBody.error || 'Unable to load site'); setSite(siteBody.site); setPolicy(policyBody.policy); setDeliveries(deliveryBody.deliveries || []); setWebhooks(webhookBody.webhooks || []); }
  useEffect(() => { load().catch((e) => setError(e.message)); }, [siteId]);
  async function testSlack() { setTesting(true); setError(''); setMessage(''); try { const r = await fetch('/api/alert-deliveries/test', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ siteId: Number(siteId) }) }); const b = await r.json(); if (!r.ok) throw new Error(b.error || 'Slack test failed'); setMessage(b.message); } catch (e) { setError(e instanceof Error ? e.message : 'Slack test failed'); } finally { setTesting(false); } }
  async function saveWebhook() { setError(''); setMessage(''); try { const r = await fetch('/api/webhooks', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ siteId: Number(siteId), url, secret, eventTypes: ['alert'] }) }); const b = await r.json(); if (!r.ok) throw new Error(b.error || 'Unable to save webhook'); setUrl(''); setSecret(''); setMessage('Signed webhook saved.'); await load(); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to save webhook'); } }
  async function removeWebhook(id: number) { await fetch(`/api/webhooks?siteId=${siteId}&id=${id}`, { method: 'DELETE' }); await load(); }
  if (!siteId) return <div className="card p-8 text-sm text-ink-500">Select a site first.</div>;
  return <div className="fade-in max-w-6xl space-y-6"><section className="rounded-2xl bg-ink-950 text-white p-6"><div className="text-xs uppercase tracking-[0.2em] text-brand-300">Integrations</div><h2 className="text-3xl font-semibold mt-2">Make every alert reach the right person.</h2><p className="text-sm text-ink-300 mt-3 max-w-2xl">Slack is configured once at the service level in Render. GAfix then sends only high-priority incidents in real time and compiles the rest into a daily evidence report.</p></section>{message && <div className="rounded-xl border border-green-200 bg-green-50 p-3 text-sm text-green-800">{message}</div>}{error && <div className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-800">{error}</div>}<section className="grid lg:grid-cols-[1.15fr_0.85fr] gap-5"><div className="card p-6"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-semibold">Slack incident channel</h3><p className="text-sm text-ink-500 mt-1">No URL needs to be pasted into this dashboard when the Render environment variable is present.</p></div><span className={`pill ${site?.global_slack_configured || site?.site_slack_configured ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>{site?.global_slack_configured || site?.site_slack_configured ? 'Configured' : 'Not configured'}</span></div><div className="mt-5 rounded-xl border border-ink-200 p-4 space-y-3 text-sm"><div className="flex items-center justify-between"><span className="text-ink-500">Global Render webhook</span><strong>{site?.global_slack_configured ? 'Available' : 'Missing'}</strong></div><div className="flex items-center justify-between"><span className="text-ink-500">Site-specific override</span><strong>{site?.site_slack_configured ? 'Available' : 'Not used'}</strong></div><div className="flex items-center justify-between"><span className="text-ink-500">Realtime threshold</span><strong className="capitalize">{policy?.realtime_min_severity || 'critical'}</strong></div></div><button onClick={testSlack} disabled={testing || !(site?.global_slack_configured || site?.site_slack_configured)} className="mt-5 rounded-lg bg-brand-600 text-white px-4 py-2.5 text-sm font-semibold disabled:opacity-50">{testing ? 'Sending test…' : 'Send test message to Slack'}</button><p className="text-xs text-ink-500 mt-3">The test posts one configuration message. It does not create an incident or affect your metrics.</p><div className="mt-5 rounded-xl bg-brand-50 border border-brand-100 p-4 text-sm text-brand-950"><strong>Render setup:</strong> add `SLACK_WEBHOOK_URL` with the full `https://hooks.slack.com/services/...` value, save it, and restart the service. Then return here and press the test button.</div></div><div className="card p-6"><h3 className="text-lg font-semibold">Routing policy</h3><p className="text-sm text-ink-500 mt-1">Current delivery behavior for this site.</p><div className="mt-5 space-y-3">{[['Realtime Slack', policy?.realtime_min_severity || 'critical'], ['Daily digest', policy?.digest_enabled === false ? 'Off' : `On · ${policy?.digest_hour ?? 9}:00 UTC`], ['Digest contents', 'Events, duplicates, confirmed blocks, gaps, failures, causes']].map(([label, value]) => <div key={label} className="border-b border-ink-100 pb-3 last:border-0"><div className="text-xs uppercase tracking-wide text-ink-400">{label}</div><div className="text-sm font-medium text-ink-800 mt-1 capitalize">{value}</div></div>)}</div><Link href={`/dashboard/settings?siteId=${siteId}`} className="inline-block mt-5 text-sm font-semibold text-brand-600">Edit alert policy →</Link></div></section><section className="card p-6"><div className="flex items-start justify-between"><div><h3 className="text-lg font-semibold">Signed operational webhook</h3><p className="text-sm text-ink-500 mt-1">Use this for PagerDuty, an internal service, or a ticketing system. Every delivery includes an HMAC signature.</p></div><span className="text-xs text-ink-400">Optional</span></div><div className="grid md:grid-cols-2 gap-3 mt-4"><input className="border rounded-lg px-3 py-2 text-sm" placeholder="https://ops.example.com/gafix" value={url} onChange={(e) => setUrl(e.target.value)} /><input className="border rounded-lg px-3 py-2 text-sm" placeholder="Signing secret" type="password" value={secret} onChange={(e) => setSecret(e.target.value)} /></div><button onClick={saveWebhook} className="mt-3 rounded-lg bg-ink-950 text-white px-4 py-2 text-sm font-semibold">Save webhook</button>{webhooks.length ? <div className="mt-4 divide-y divide-ink-100">{webhooks.map((hook) => <div key={hook.id} className="py-3 flex items-center justify-between gap-3"><span className="mono text-sm break-all">{hook.url}</span><button onClick={() => removeWebhook(hook.id)} className="text-xs text-red-600">Remove</button></div>)}</div> : <p className="text-sm text-ink-400 mt-4">No signed webhooks configured.</p>}</section><section className="card p-6"><h3 className="text-lg font-semibold">Evidence exports</h3><p className="text-sm text-ink-500 mt-1">Download operational evidence for a review, incident postmortem, or CEO update.</p><div className="flex flex-wrap gap-2 mt-4">{['events','alerts','revenue','synthetic'].map((kind) => <a key={kind} href={`/api/export?siteId=${encodeURIComponent(siteId || '')}&kind=${kind}&format=csv`} className="rounded-lg border border-ink-300 px-3 py-2 text-sm hover:bg-ink-50">Export {kind} CSV</a>)}</div></section><section className="card p-6"><h3 className="text-lg font-semibold">Recent delivery activity</h3><div className="mt-4 divide-y divide-ink-100">{deliveries.slice(0, 8).map((item: any) => <div key={item.id} className="py-3 flex items-center justify-between text-sm"><span className="capitalize">{item.channel} · {item.event_name || item.code}</span><span className={`pill ${item.status === 'delivered' ? 'bg-green-100 text-green-800' : item.status === 'failed' ? 'bg-red-100 text-red-800' : 'bg-amber-100 text-amber-800'}`}>{item.status}</span></div>)}{!deliveries.length && <p className="text-sm text-ink-400">No realtime deliveries yet. A daily report will appear after the first complete reporting window.</p>}</div></section></div>;
}
