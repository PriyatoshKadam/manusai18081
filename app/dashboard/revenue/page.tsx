'use client';

import { useEffect, useState } from 'react';
import { useSearchParams } from 'next/navigation';
import { CommandKpi, DashboardSection } from '../command-visuals';

function n(value: unknown) { return Number(value || 0).toLocaleString(); }

export default function RevenuePage() {
  const search = useSearchParams(); const siteId = search.get('siteId'); const [data, setData] = useState<any>(null);
  useEffect(() => { if (!siteId) return; let active = true; async function load() { try { const response = await fetch(`/api/tag-health?siteId=${encodeURIComponent(siteId)}`, { cache: 'no-store' }); if (response.ok && active) setData(await response.json()); } catch {} } load(); const timer = setInterval(load, 15000); return () => { active = false; clearInterval(timer); }; }, [siteId]);
  if (!siteId) return <div className="text-sm text-slate-500">Select a site.</div>; if (!data) return <div className="text-sm text-slate-500">Loading revenue tracking…</div>;
  const revenue = data.revenue || []; const mismatch = revenue.filter((row: any) => ['value_mismatch', 'missing_vendor', 'currency_mismatch', 'invalid_value', 'missing', 'duplicate'].includes(row.status)); const totalDelta = revenue.reduce((sum: number, row: any) => sum + Math.abs(Number(row.delta_value || 0)), 0); const matched = revenue.filter((row: any) => row.status === 'matched').length;
  return <div className="fade-in mx-auto max-w-[1500px] space-y-7"><div className="dashboard-section-head"><div><div className="dashboard-eyebrow">Revenue tracking · Last 24 hours</div><h2>See where tracking could affect revenue</h2><p>Compare purchase tracking from your tools and find missing, repeated, or different revenue values.</p></div><div className="dashboard-top-control"><span className="dot bg-[#a8f06a]" /><strong>Last 24h</strong> · visitor tracking</div></div><div className="grid gap-3 md:grid-cols-4"><CommandKpi label="Purchases seen" value={n(revenue.length)} note="Purchase records GAfix found" tone="blue" /><CommandKpi label="Matching records" value={n(matched)} note="Purchase details agree" tone="lime" /><CommandKpi label="Purchases to review" value={n(mismatch.length)} note="Missing tools, invalid, or different details" tone={mismatch.length ? 'rose' : 'lime'} /><CommandKpi label="Value difference" value={n(totalDelta)} note="Total difference found" tone={totalDelta ? 'amber' : 'lime'} /></div><section className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><div className="rounded-2xl border border-white/[.08] bg-[#111722] p-5 lg:p-6"><DashboardSection eyebrow="Purchase tracking check" title="How reliable the purchase data looks" description="This compares tracking records; it is not a replacement for your payment or accounting system." /><div className="mx-auto mt-4 grid h-56 w-56 place-items-center rounded-full border-[18px] border-[#202b3b]" style={{ background: `conic-gradient(#a8f06a ${revenue.length ? (matched / revenue.length) * 360 : 0}deg, #ff718d 0deg)` }}><div className="grid h-36 w-36 place-items-center rounded-full bg-[#111722] text-center"><div><div className="text-3xl font-semibold text-white">{revenue.length ? Math.round((matched / revenue.length) * 100) : 0}%</div><div className="mt-1 text-[10px] uppercase tracking-wider text-slate-500">records match</div></div></div></div><div className="mt-5 flex justify-center gap-4 text-xs"><span className="text-[#a8f06a]">● matches</span><span className="text-[#ff718d]">● needs review</span></div></div><div className="rounded-2xl border border-white/[.08] bg-[#111722] p-5 lg:p-6"><DashboardSection eyebrow="Purchase details" title="Purchase tracking records" description="Open a record to investigate differences in the tracking setup." />{revenue.length ? <div className="space-y-3">{revenue.slice(0, 12).map((row: any, index: number) => <div key={row.transaction_id || index} className="grid grid-cols-[minmax(130px,1fr)_100px_120px] items-center gap-3 rounded-xl border border-white/[.06] bg-[#0c121c] p-3 text-xs"><div><div className="font-mono font-medium text-slate-200">{row.transaction_id || 'Order reference not found'}</div><div className="mt-1 text-slate-500">{row.currency === 'MIXED' ? 'Different currencies reported' : row.currency || 'Currency not found'}</div></div><span className={`pill w-fit ${row.status !== 'matched' ? 'bg-[#ff718d]/10 text-[#ff9aae]' : 'bg-[#a8f06a]/10 text-[#b9f57e]'}`}>{row.status === 'value_mismatch' ? 'Value is different' : row.status === 'missing_vendor' ? 'Tool not reached' : row.status === 'currency_mismatch' ? 'Currencies differ' : row.status === 'invalid_value' ? 'Value is not valid' : row.status === 'missing' ? 'Details missing' : row.status === 'duplicate' ? 'Possible repeat' : row.status === 'single_vendor' ? 'One tool observed' : row.status === 'observed_unconfigured' ? 'Tool not configured' : 'Looks matched'}</span><div className="text-right font-mono text-slate-400">{row.delta_value === null || row.delta_value === undefined ? 'No safe comparison' : `${row.currency || ''} ${row.delta_value}`}</div></div>)}</div> : <div className="empty-visual">No purchase tracking records yet. GAfix will show them after a purchase is tracked.</div>}</div></section></div>;
}
